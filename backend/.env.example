# =============================================================================
# Example environment variables
# =============================================================================
# Template for required environment variables.
# Copy to the appropriate .env file:
#   cp .env.example .env
#
# Then edit the copied file and replace placeholder values.
#
# =============================================================================


# ========== Database ==========
# # if launch from docker-compose, this variable will be automatically set up
# DATABASE_URL=postgresql://postgres_user:postgres_password@localhost:postgres_port/postgres_dbname

# ========== Frontend ==========
FRONTEND_URL=http://localhost:3000        # local development (<http://localhost:frontend_port>)
# FRONTEND_URL=https://your-frontend.com  # production